spring:
  datasource:
    url: jdbc:h2:mem:scheduler-${random.uuid};MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
scheduler:
  clock:
    fixed-instant: "2026-08-19T08:00:00Z"